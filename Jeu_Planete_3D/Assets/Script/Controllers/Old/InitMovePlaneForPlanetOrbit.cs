/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitMovePlaneForPlanetOrbit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Orbit parentOrbit = gameObject.GetComponentInParent<Orbit>();
        transform.localScale = new Vector3(parentOrbit.orbitPath.xAxis / 2, transform.localScale.y,
            parentOrbit.orbitPath.yAxis / 2);
        transform.localPosition = new Vector3(0, 0, 0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/
